language: generic
env:
  global:
  - TESTMODEL_URL=http://localhost:8080/intermine-demo
  - TESTMODEL_PATH=intermine-demo
sudo: true
jdk:
  - openjdk8
script:
  - jdk_switcher use openjdk8
  - bash init-biotestmine.sh
  - bash init-testmine.sh
  - echo "successfully built"
